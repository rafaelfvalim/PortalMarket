<%= render 'layouts/messages' %>
<div class="wrapper wrapper-content animated fadeIn">
  <div class="row">
    <div class="col-md-12">
      <%= link_to :back, class: 'btn btn-primary next' do %>
          <i class="fa fa-chevron-left"> <%= t 'labels.buttons.back' %> </i>
      <% end %>
      <%= link_to new_landing_text_path , class: 'btn btn-primary' do %>
          <i class="fa fa-plus"> <%= t 'labels.buttons.new' %> </i>
      <% end %>
    </div>
  </div>
  <br>
  <h3>Textos da landing page </h3>
  <div class="row">
    <div class="col-md-12">
      <div class="panel panel-default center">
        <div class="panel-body">
          <table class="table table-striped table-condensed display responsive no-wrap " id="landing-texts-table" data-source="<%= texts_ajax_landing_texts_path(format: :json) %>">
            <thead>
            <tr>
              <th>Location Name</th>
              <th></th>
              <th></th>
            </tr>
            </thead>
            <tbody>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>