<%= simple_form_for(@landing_page_image) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :image %>
  </div>

  <div class="form-actions">
    <%= f.submit 'Salvar', class:'btn btn-primary' %>
  </div>

<% end %>
