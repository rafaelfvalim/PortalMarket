<%= form_for(@related_script) do |f| %>
  <% if @related_script.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@related_script.errors.count, "error") %> prohibited this related_script from being saved:</h2>

      <ul>
      <% @related_script.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :related_script %><br>
    <%= f.number_field :related_script %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
