<div class="wrapper wrapper-content animated fadeIn">
  <div class="row">
    <div class="col-md-12">
      <%= render 'layouts/process_tracker' %>
      <div class="row">
        <div class="col-md-12">
          <div class="pull-left">
            <%= link_to previous_wizard_path(script_id: @script.id), :class => 'btn btn-primary' do %>
                <i class="fa fa-chevron-left"></i> Back
            <% end %>
          </div>
          <div class="pull-right">
            <%= link_to next_wizard_path(script_id: @script.id), class: 'btn btn-primary next' do %>
                <i class="fa fa-chevron-right"></i> Next
            <% end %>
          </div>
        </div>
      </div>
      <br>

      <div class="row">
        <div class="col-md-12">
          <div class="ibox float-e-margins">
            <div class="ibox-title">
              <h5>Classificações
                <span class="badge" id="process_badge"></span>
              </h5>

              <div class="ibox-tools">
                <a class="collapse-link">
                  <i class="fa fa-chevron-up"></i>
                </a>
              </div>
            </div>
            <div class="ibox-content">
              <%= render 'breadcrumb_value_chain' %>
            </div>
          </div>
        </div>
      </div>
      <div class="row_panel_build">
        <!--Row-->
        <input type="hidden" id="step" value="step1">

        <div class="row">
          <% @process_modules.each do |process| %>
              <div class="col-md-3">
                <div class="ibox">
                  <div class="ibox-content product-box">
                    <% if process.image.present? %>
                        <div class="process_image">
                          <img class="process_image" src="<%= process.image.thumb.url.to_s %>"/>
                        </div>
                    <% else %>
                        <div class="product-imitation">
                          [IMAGE]
                        </div>
                    <% end %>
                    <div class="product-desc">
                      <small class="text-muted">Category</small>
                      <%= link_to process.description, classification_wizard_scripts_path(process_id: process.id, script_id: @script.id), :class=> 'product-name' %>
                      <div class="small m-t-xs">
                        <%= process.long_description %>
                      </div>
                      <div class="m-t text-righ">
                        <%= link_to classification_wizard_scripts_path(process_id: process.id, script_id: @script.id), :class => 'btn btn-xs btn-outline btn-primary' do %>
                            Go <i class="fa fa-long-arrow-right"></i>
                        <% end %>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>



