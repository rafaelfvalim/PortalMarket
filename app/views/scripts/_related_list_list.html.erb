<% Script.includes(:related_scripts).where('id = ?', @script.id).each do |f| %>
    <% f.related_scripts.each do |related_script| %>
        <tr id="<%= related_script.id %>">
          <td ><%= related_script.script.description %></td>
          <td class="span1"><%= link_to "Destroy", {controller: "related_scripts", action: "destroy_ajax", id: related_script.id}, class: "btn btn-info", :remote => true, :onclick => "remove_table_row(this)" %></td>
        </tr>
    <% end %>
<% end %>